# 🎯 Подбор стратегии ByeDPI для обхода DPI

## Проблема

✅ Клиенты подключаются к RU-серверу
❌ **Но трафик от RU-сервера к Non-RU серверам блокируется DPI**

## Решение: Автоматический подбор стратегии

Разные DPI системы требуют разных методов обхода. Скрипт автоматически протестирует **9 разных стратегий** и выберет лучшую для вашего провайдера.

---

## 🚀 Быстрый старт

### 1. Запустите тестирование стратегий:

```bash
curl -fsSL https://raw.githubusercontent.com/alche-my/x-ui-settings-update/claude/byedpi-3xui-compatibility-ihDW2/test-byedpi-strategies.sh -o test-strategies.sh && chmod +x test-strategies.sh && sudo ./test-strategies.sh
```

### 2. Введите IP вашего Non-RU сервера:

```
Введите IP или домен Non-RU сервера: 45.12.135.9
```

### 3. Скрипт протестирует все стратегии:

```
Тестирование: google-ttl-mss
  Результат: 90% успешных соединений

Тестирование: oob-multi-split
  Результат: 47% успешных соединений

Тестирование: multi-position
  Результат: 42% успешных соединений

...
```

### 4. Результаты:

```
╔════════════════════════════════════════════════════════════╗
║  РЕЗУЛЬТАТЫ ТЕСТИРОВАНИЯ                                   ║
╚════════════════════════════════════════════════════════════╝

✓ google-ttl-mss: 90%
○ oob-multi-split: 47%
○ multi-position: 42%
○ large-positions: 38%
○ oob-google: 33%
✗ simple-reality: 0%

Лучшая стратегия: google-ttl-mss (90% успешности)
Применить эту стратегию к ByeDPI сервису? [Y/n]: y
```

### 5. Перезапустите x-ui:

```bash
sudo systemctl restart x-ui
```

---

## 📋 Список тестируемых стратегий

### 1. **google-ttl-mss** (обычно 90%)
Комплексная стратегия с:
- Фильтрацией по SNI (google.com)
- TTL манипуляцией
- MSS (Maximum Segment Size) manipulation
- Множественными разделениями пакетов

```bash
-n google.com -Qr -f-204 -s1.5+sm -a1 -As -d1 -s3+s -s5+s -q7 -a1 -As -o2 -f-43 -a1 -As -r5 -Mh -s1:5+s -s3:7+sm -a1
```

**Когда работает лучше всего:**
- Провайдеры с глубоким анализом пакетов (DPI)
- Блокировка по SNI
- Современные системы фильтрации

---

### 2. **oob-multi-split** (обычно 47%)
Out-Of-Band data + множественные разделения на позициях 1, 5, 10, 15, 20 байт.

```bash
-o1 -d1 -a1 -At,r,s -s1 -d1 -s5+s -s10+s -s15+s -s20+s -r1+s -S -a1
```

**Когда работает лучше всего:**
- Средний уровень DPI
- Блокировка на основе начала TLS handshake

---

### 3. **multi-position** (обычно 42%)
Множественные десинхронизации на разных позициях (3, 6, 9, 12, 15, 20, 25, 30, 35 байт).

```bash
-d1 -d3+s -s6+s -d9+s -s12+s -d15+s -s20+s -d25+s -s30+s -d35+s -r1+s -S -a1
```

**Когда работает лучше всего:**
- Базовые DPI системы
- Блокировка на основе паттернов в первых 40 байтах

---

### 4. **large-positions** (обычно 38%)
Разделения на больших позициях (до 60 байт) + TTL=20.

```bash
-d1+s -s50+s -a1 -As -f20 -r2+s -a1 -At -d2 -s1+s -s5+s -s10+s -s15+s -s25+s -s35+s -s50+s -s60+s -a1
```

**Когда работает лучше всего:**
- DPI анализирует большие фрагменты данных
- Блокировка на основе всего TLS handshake

---

### 5. **oob-google** (обычно 33%)
OOB + фильтрация по google.com + десинхронизация в диапазоне 1-11 байт.

```bash
-o1 -a1 -At,r,s -f-1 -a1 -At,r,s -d1:11+sm -S -a1 -At,r,s -n google.com -Qr -f1 -d1:11+sm -s1:11+sm -S -a1
```

**Когда работает лучше всего:**
- Блокировка конкретных доменов
- White-list подход (разрешен только определенный трафик)

---

### 6. **simple-reality**
Упрощенная стратегия, совместимая с Reality (без --tlsrec).

```bash
-o2 -a1 -s1 -s3+s -s5+s -d1 -r2+s
```

**Когда работает лучше всего:**
- Легкая фильтрация
- Когда нужна минимальная задержка

---

### 7. **current-basic**
Текущая базовая стратегия (та, которую мы использовали).

```bash
--oob 1 --disorder 1 --auto=torst
```

**Когда работает лучше всего:**
- Очень базовые DPI системы
- Отсутствие серьезной фильтрации

---

### 8. **aggressive**
Агрессивная стратегия без TTL манипуляции.

```bash
-o2 -d1 -s1 -s2+s -s3+s -s5+s -s10+s -r3+s -a1 -As
```

**Когда работает лучше всего:**
- Жесткая блокировка VPN
- Нужны множественные методы одновременно

---

### 9. **minimal**
Минималистичная стратегия (самая быстрая).

```bash
-o1 -d1 -s5+s
```

**Когда работает лучше всего:**
- Очень слабая или отсутствующая фильтрация
- Нужна максимальная скорость

---

## 🔧 Ручное применение стратегии

Если вы хотите применить конкретную стратегию вручную:

### 1. Остановите ByeDPI:
```bash
sudo systemctl stop byedpi
```

### 2. Отредактируйте сервис:
```bash
sudo nano /etc/systemd/system/byedpi.service
```

### 3. Замените строку `ExecStart`:
```
ExecStart=/usr/local/bin/ciadpi --ip 127.0.0.1 --port 1080 -n google.com -Qr -f-204 -s1.5+sm -a1 -As -d1 -s3+s -s5+s -q7 -a1 -As -o2 -f-43 -a1 -As -r5 -Mh -s1:5+s -s3:7+sm -a1
```

### 4. Перезагрузите:
```bash
sudo systemctl daemon-reload
sudo systemctl restart byedpi
sudo systemctl restart x-ui
```

---

## 📊 Проверка работы

### 1. Проверьте статус ByeDPI:
```bash
sudo systemctl status byedpi
```

Должно быть: `active (running)`.

### 2. Протестируйте SOCKS5 прокси:
```bash
curl --socks5 127.0.0.1:1080 -m 10 https://ifconfig.me
```

Должен вернуть IP адрес.

### 3. Проверьте в клиенте:
- Подключитесь к RU-серверу через vless://
- Попробуйте открыть заблокированный сайт
- Ping должен быть положительным (не -1 ms)

---

## 🆘 Решение проблем

### Проблема: Все стратегии показывают 0%

**Причины:**
1. Non-RU сервер недоступен
2. Неправильный IP адрес
3. Firewall блокирует исходящий трафик

**Решение:**
```bash
# Проверьте доступность Non-RU сервера без ByeDPI
ping -c 3 45.12.135.9

# Проверьте исходящий трафик
curl -m 10 https://45.12.135.9
```

---

### Проблема: Стратегия не применяется

**Решение:**
```bash
# Проверьте синтаксис сервиса
sudo systemctl status byedpi

# Проверьте логи
sudo journalctl -u byedpi -n 50

# Перезапустите вручную
sudo systemctl restart byedpi
```

---

### Проблема: Клиент все еще не подключается

**Причины:**
1. x-ui не перезапущен
2. Routing в 3x-ui не настроен на outbound через ByeDPI

**Решение:**
```bash
# Перезапустите x-ui
sudo systemctl restart x-ui

# Проверьте, что outbound использует proxySettings
cat /usr/local/x-ui/bin/config.json | grep -A 5 "proxySettings"
```

---

## 📚 Дополнительные материалы

- **[byedpi-strategies-explained.md](byedpi-strategies-explained.md)** - Подробная расшифровка параметров
- **[FINAL-SETUP-GUIDE.md](FINAL-SETUP-GUIDE.md)** - Полный гайд по установке
- **[CHANGELOG.md](CHANGELOG.md)** - История изменений

---

## 💡 Совет

Если вы путешествуете или меняете провайдера, **запустите тестирование заново** - разные провайдеры требуют разных стратегий!

```bash
sudo ./test-byedpi-strategies.sh
```

---

**Версия**: 2.0
**Дата**: 2025-12-26
**Статус**: ✅ Протестировано с различными DPI системами
