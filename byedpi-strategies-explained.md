# ByeDPI Strategies: Расшифровка параметров

## Что показывает скриншот

Мобильное приложение ByeDPI тестирует разные **комбинации методов обхода DPI** и показывает процент успешности для вашего провайдера.

---

## 📋 Расшифровка параметров ByeDPI

### Основные методы обхода DPI

| Параметр | Значение | Описание |
|----------|----------|----------|
| `-d` / `--desync` | число | Задержка перед отправкой пакета (десинхронизация) |
| `-s` / `--split` | позиция+s | Разделение пакета на части в указанной позиции |
| `-o` / `--oob` | 1/2 | Out-Of-Band data (вставка "срочных" данных) |
| `-r` / `--disorder` | число+s | Изменение порядка отправки пакетов |
| `-a` / `--auto` | число | Автоопределение позиции разделения |
| `-A` / `--multiattach` | t,r,s | Применить к TLS (t), TCP (r), SSL (s) |
| `-S` / `--md5sig` | - | Добавить MD5 подпись к TCP пакетам |
| `-n` | домен | SNI для фильтрации (например, google.com) |
| `-Q` / `--qf` | r/f | Quick filter (r - reverse) |
| `-f` / `--ttl` | число | TTL манипуляция |
| `-M` / `--mss` | h | MSS (Maximum Segment Size) manipulation |

### Примеры из скриншота

**Стратегия 1 (42%):**
```bash
-d1 -d3+s -s6+s -d9+s -s12+s -d15+s -s20+s -d25+s -d30+s -d35+s -r1+s -S -a1
```
Множественные разделения на разных позициях (3, 6, 9, 12, 15, 20, 25, 30, 35 байт).

**Стратегия 2 (47%):**
```bash
-o1 -d1 -a1 -At,r,s -s1 -d1 -s5+s -s10+s -s15+s -s20+s -r1+s -S -a1
```
OOB + разделения на позициях 1, 5, 10, 15, 20 + применение ко всем типам трафика.

**Стратегия 3 (90%):** 🎯 **ЛУЧШАЯ**
```bash
-n google.com -Qr -f-204 -s1.5+sm -a1 -As -d1 -s3+s -s5+s -q7 -a1 -As -o2 -f-43 -a1 -As -r5 -Mh -s1:5+s -s3:7+sm -a1
```
Комплексная стратегия с:
- Фильтрацией по SNI (google.com)
- TTL манипуляцией (-f-204, -f-43)
- Множественными разделениями
- MSS manipulation (-Mh)
- OOB2

**Стратегия 4 (38%):**
```bash
-d1+s -s50+s -a1 -As -f20 -r2+s -a1 -At -d2 -s1+s -s5+s -s10+s -s15+s -s25+s -s35+s -s50+s -s60+s -a1
```
Разделение на больших позициях (до 60 байт) + TTL=20.

**Стратегия 5 (33%):**
```bash
-o1 -a1 -At,r,s -f-1 -a1 -At,r,s -d1:11+sm -S -a1 -At,r,s -n google.com -Qr -f1 -d1:11+sm -s1:11+sm -S -a1
```
OOB + десинхронизация в диапазоне 1-11 байт + фильтрация по google.com.

---

## 🎯 Проблема с вашим сервером

### Текущая ситуация:
- ✅ Клиенты подключаются к RU-серверу
- ❌ Трафик от RU-сервера к Non-RU серверам **блокируется DPI**

### Текущие параметры ByeDPI (которые НЕ работают):
```bash
--oob 1 --disorder 1 --auto=torst
```

### Почему не работает:
Эти параметры **слишком простые** для вашего DPI оборудования. Нужна более сложная стратегия.

---

## 🛠️ Решение: Автоматический подбор стратегии

Создам скрипт, который:
1. Протестирует разные комбинации параметров ByeDPI
2. Проверит соединение к Non-RU серверу через каждую стратегию
3. Выберет лучшую (с наибольшим процентом успеха)
4. Автоматически обновит systemd сервис

---

## 📊 Рекомендуемые стратегии для тестирования

На основе скриншота, начнем с самой успешной (90%):

### Стратегия #1 (google.com + TTL + MSS)
```bash
-n google.com -Qr -f-204 -s1.5+sm -a1 -As -d1 -s3+s -s5+s -q7 -a1 -As -o2 -f-43 -a1 -As -r5 -Mh -s1:5+s -s3:7+sm -a1
```

### Стратегия #2 (OOB + множественные разделения)
```bash
-o1 -d1 -a1 -At,r,s -s1 -d1 -s5+s -s10+s -s15+s -s20+s -r1+s -S -a1
```

### Стратегия #3 (большие позиции)
```bash
-d1+s -s50+s -a1 -As -f20 -r2+s -a1 -At -d2 -s1+s -s5+s -s10+s -s15+s -s25+s -s35+s -s50+s -s60+s -a1
```

### Стратегия #4 (упрощенная для Reality)
```bash
-o2 -a1 -s1 -s3+s -s5+s -d1 -r2+s
```
Без `--tlsrec` для совместимости с Reality!

---

## 🚀 Следующий шаг

Запустим скрипт автоматического подбора стратегии, который:
1. Протестирует каждую стратегию
2. Проверит соединение к вашему Non-RU серверу
3. Выберет лучшую
4. Обновит ByeDPI сервис

Готовлю скрипт...
