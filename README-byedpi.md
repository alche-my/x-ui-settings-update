# ByeDPI Setup для 3x-ui - Обход DPI на RU серверах

## 🎯 Назначение

Этот скрипт автоматически устанавливает и настраивает **ByeDPI** на RU-сервере для обхода DPI при каскадной архитектуре VPN.

## 📊 Архитектура решения

```
┌──────────────┐     ┌─────────────────────────────────┐     ┌──────────────┐
│ Клиент в РФ  │────▶│  RU-сервер (этот скрипт)        │────▶│ Non-RU VPS   │
│              │     │  ┌─────────┐    ┌────────────┐  │     │              │
│  V2rayNG     │     │  │ 3x-ui   │───▶│  ByeDPI    │──┼────▶│ 3x-ui / Xray │
│  V2rayN      │     │  │ Inbound │    │  SOCKS5    │  │     │              │
│  Streisand   │     │  └─────────┘    └────────────┘  │     │              │
│              │     │                                 │     │              │
│              │     │  Xray outbound → ByeDPI SOCKS5 │     │              │
└──────────────┘     └─────────────────────────────────┘     └──────────────┘
```

### Как это работает:

1. **Клиент в РФ** подключается к вашему **RU-серверу** (обычное соединение, не блокируется)
2. **RU-сервер (3x-ui)** принимает трафик от клиентов
3. **ByeDPI** фрагментирует и обфусцирует исходящие пакеты к Non-RU серверу
4. **DPI в РФ** не может распознать паттерн → пропускает трафик
5. **Non-RU сервер** получает трафик и отправляет в интернет

## 🚀 Быстрый старт

### Предварительные требования

- Ubuntu 22.04 / 24.04 (или Debian)
- Установленный 3x-ui на RU-сервере
- Доступ к Non-RU серверу (IP, порт, UUID)
- Root доступ

### Установка

```bash
# 1. Скачайте скрипт на RU-сервер
wget https://raw.githubusercontent.com/your-repo/x-ui-settings-update/main/setup-byedpi-proxy.sh
chmod +x setup-byedpi-proxy.sh

# 2. Запустите с параметрами
sudo ./setup-byedpi-proxy.sh \
  --non-ru-ip 1.2.3.4 \
  --non-ru-port 443 \
  --non-ru-uuid "xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx"

# 3. Или запустите в интерактивном режиме
sudo ./setup-byedpi-proxy.sh
```

## 📋 Параметры скрипта

| Параметр | Описание | Обязательный | По умолчанию |
|----------|----------|--------------|--------------|
| `--non-ru-ip` | IP адрес Non-RU сервера | ✅ Да | - |
| `--non-ru-uuid` | UUID для VLESS подключения | ✅ Да | - |
| `--non-ru-port` | Порт Non-RU сервера | ❌ Нет | 443 |
| `--byedpi-port` | Порт SOCKS5 прокси ByeDPI | ❌ Нет | 1080 |
| `--non-interactive` | Без интерактивных запросов | ❌ Нет | false |
| `--uninstall` | Удалить ByeDPI | ❌ Нет | false |

## 🔧 Что делает скрипт

1. ✅ Устанавливает зависимости (gcc, make, git)
2. ✅ Клонирует и компилирует ByeDPI
3. ✅ Создает systemd сервис для автозапуска
4. ✅ Запускает ByeDPI SOCKS5 прокси на порту 1080
5. ✅ Генерирует готовую конфигурацию Xray
6. ✅ Создает подробную инструкцию по настройке

## 📝 После установки

Скрипт создаст конфигурационные файлы в `/root/byedpi-config/`:

- `xray-outbound-config.json` - готовая конфигурация для Xray
- `SETUP-INSTRUCTIONS.md` - подробная инструкция по настройке

### Следующие шаги:

1. **Откройте инструкцию:**
   ```bash
   cat /root/byedpi-config/SETUP-INSTRUCTIONS.md
   ```

2. **Настройте 3x-ui:**
   - Вариант 1: Скопируйте JSON из `xray-outbound-config.json` в конфигурацию Xray
   - Вариант 2: Создайте outbound-ы вручную через веб-интерфейс

3. **Перезапустите 3x-ui:**
   ```bash
   sudo systemctl restart x-ui
   ```

## ✅ Проверка работы

### Проверить ByeDPI

```bash
# Статус сервиса
sudo systemctl status byedpi

# Логи в реальном времени
sudo journalctl -u byedpi -f

# Проверить SOCKS5 прокси
curl --socks5 127.0.0.1:1080 https://www.google.com
```

### Проверить 3x-ui

```bash
# Перезапустить
sudo systemctl restart x-ui

# Статус
sudo systemctl status x-ui

# Логи
sudo journalctl -u x-ui -f
```

## 🔧 Настройка параметров ByeDPI

Если стандартные параметры не работают с вашим провайдером, можно изменить:

```bash
# Редактировать сервис
sudo nano /etc/systemd/system/byedpi.service
```

Измените строку `ExecStart`:

### Для мобильных операторов (МТС, Билайн, Мегафон)
```
ExecStart=/usr/local/bin/ciadpi --port 1080 --split 2 --disorder 1 --fake
```

### Для проводных провайдеров (Ростелеком, МГТС)
```
ExecStart=/usr/local/bin/ciadpi --port 1080 --tlsrec 1+s --split-pos 2
```

### Агрессивный режим (если ничего не помогает)
```
ExecStart=/usr/local/bin/ciadpi --port 1080 --disorder 3 --split 3 --tlsrec 1+s --fake --auto=torst
```

После изменений:
```bash
sudo systemctl daemon-reload
sudo systemctl restart byedpi
```

## 📖 Пример настройки 3x-ui через веб-интерфейс

### Вариант 1: Через JSON конфигурацию (проще)

1. Откройте панель 3x-ui в браузере
2. Перейдите в **Panel Settings → Xray Configs** (или **Config**)
3. Найдите секцию `"outbounds": [...]`
4. Добавьте содержимое файла `/root/byedpi-config/xray-outbound-config.json`
5. Нажмите **Save** и **Restart Xray**

### Вариант 2: Через веб-интерфейс вручную

#### Шаг 1: Создать SOCKS5 Outbound

1. В панели 3x-ui → **Outbounds** → **Add Outbound**
2. Заполните:
   ```
   Tag: byedpi-socks
   Protocol: SOCKS
   Address: 127.0.0.1
   Port: 1080
   ```
3. Save

#### Шаг 2: Создать VLESS Outbound через ByeDPI

1. **Outbounds** → **Add Outbound**
2. Заполните:
   ```
   Tag: non-ru-via-byedpi
   Protocol: VLESS
   Address: [IP Non-RU сервера]
   Port: 443
   UUID: [UUID Non-RU сервера]
   Encryption: none
   Network: tcp
   Security: tls
   ```
3. В **Advanced Settings** → **Proxy Settings**:
   ```
   Tag: byedpi-socks
   ```
4. Save

#### Шаг 3: Настроить роутинг

1. **Routing Rules** → создайте правило для направления трафика через `non-ru-via-byedpi`
2. Или установите `non-ru-via-byedpi` как **Default Outbound**

## 🗑️ Удаление

```bash
sudo ./setup-byedpi-proxy.sh --uninstall
```

Это удалит:
- ByeDPI сервис
- Бинарный файл
- Конфигурационные файлы
- Исходники

## 🔍 Диагностика проблем

### ByeDPI не запускается

```bash
# Логи с ошибками
sudo journalctl -u byedpi --no-pager -n 50

# Проверить, что порт свободен
sudo netstat -tlnp | grep 1080

# Убить процесс, если порт занят
sudo lsof -ti:1080 | xargs kill -9

# Перезапустить
sudo systemctl restart byedpi
```

### Xray не подключается к Non-RU серверу

1. Проверить, что ByeDPI работает:
   ```bash
   systemctl status byedpi
   ```

2. Проверить логи Xray:
   ```bash
   sudo journalctl -u x-ui -f
   ```

3. Проверить конфигурацию outbound в 3x-ui:
   - Убедитесь, что `proxySettings.tag` указывает на `byedpi-socks`
   - Проверьте правильность IP, порта и UUID Non-RU сервера

4. Тест подключения через ByeDPI:
   ```bash
   curl -v --socks5 127.0.0.1:1080 https://[NON-RU-IP]:[PORT]
   ```

### Соединение тормозит или рвется

Попробуйте другие параметры ByeDPI (см. раздел "Настройка параметров ByeDPI")

## 📚 Дополнительные ресурсы

- [ByeDPI GitHub](https://github.com/hufrea/byedpi)
- [3x-ui GitHub](https://github.com/MHSanaei/3x-ui)
- [Xray Documentation](https://xtls.github.io/)
- [Zapret (альтернатива)](https://github.com/bol-van/zapret)

## 🆘 Поддержка

Если возникли проблемы:

1. Проверьте логи: `sudo journalctl -u byedpi -n 100 --no-pager`
2. Проверьте логи 3x-ui: `sudo journalctl -u x-ui -n 100 --no-pager`
3. Откройте issue в репозитории с логами

## 📄 Лицензия

MIT License

---

**Примечание:** Этот инструмент предназначен для обхода цензуры в образовательных целях. Используйте ответственно и в соответствии с законодательством вашей страны.
