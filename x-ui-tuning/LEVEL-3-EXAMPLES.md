# Level 3 Advanced: Примеры использования

**Дата:** 26 декабря 2025

---

## 🎯 Сценарий 1: Базовая настройка с Xray Fragment

**Задача:** Настроить DPI bypass на RU VPS для исходящего соединения к Non-RU серверу

```bash
# На вашем RU VPS (ENTRY точка)
cd /root/x-ui-settings-update/x-ui-tuning

# Применить Level 3 с методом Xray Fragment
./level-3-advanced.sh \
  --method xray-fragment \
  --non-ru-ip 95.217.123.45 \
  --non-ru-port 443

# Скрипт автоматически:
# ✅ Создаст fragment outbound в Xray
# ✅ Настроит dialerProxy для VLESS outbound
# ✅ Применит базовую стратегию фрагментации (100-200 bytes)
# ✅ Протестирует соединение
```

**Результат:**
```
Client → RU VPS (3x-ui + Fragment) → Non-RU VPS → Internet
                     ↓
              DPI обходится через
              фрагментацию пакетов
```

---

## 🤖 Сценарий 2: Автоматический подбор стратегий

**Задача:** Включить автоматическое переключение стратегий при падении соединения

```bash
./level-3-advanced.sh \
  --method xray-fragment \
  --non-ru-ip 95.217.123.45 \
  --auto-strategy

# Дополнительно создаст:
# ✅ /opt/dpi-strategies.json - база из 5 стратегий
# ✅ /opt/health-check.sh - проверка соединения
# ✅ /opt/auto-strategy-selector.sh - переключатель стратегий
# ✅ Cron задачу (каждые 5 минут)
```

**Как это работает:**

1. Каждые 5 минут запускается Health Check
2. Если соединение упало → автоматически переключается на следующую стратегию
3. Если все 5 стратегий не помогли → возврат к первой
4. Логируется в `/var/log/dpi-health-check.log`

**Мониторинг:**
```bash
# Смотреть логи Health Check
tail -f /var/log/dpi-health-check.log

# Узнать текущую стратегию
cat /var/run/current-dpi-strategy

# Переключить стратегию вручную
/opt/auto-strategy-selector.sh switch-next
```

---

## 🔧 Сценарий 3: Использование ByeDPI для максимальной гибкости

**Задача:** Нужны все методы ByeDPI (disorder, split, tlsrec, etc)

```bash
./level-3-advanced.sh \
  --method byedpi \
  --non-ru-ip 95.217.123.45 \
  --auto-strategy

# Скрипт автоматически:
# ✅ Скачает и установит ByeDPI
# ✅ Создаст systemd сервис (127.0.0.1:1080)
# ✅ Настроит proxySettings в Xray
# ✅ Применит стратегию --disorder 1 --split 1 --auto=torst
```

**Архитектура:**
```
Client → RU VPS (3x-ui) → ByeDPI SOCKS5 → Non-RU VPS
                             ↓
                    DPI bypass методы:
                    - disorder
                    - split
                    - auto=torst
```

**Изменение стратегии ByeDPI вручную:**
```bash
# Остановить сервис
systemctl stop byedpi

# Отредактировать /etc/systemd/system/byedpi.service
# Изменить строку ExecStart на нужные параметры, например:
# ExecStart=/usr/local/bin/byedpi --ip 127.0.0.1 --port 1080 --tlsrec 1+s --split 1

# Перезагрузить и запустить
systemctl daemon-reload
systemctl start byedpi

# Проверить
systemctl status byedpi
```

**Популярные стратегии ByeDPI:**
```bash
# Стратегия 1: Disorder + Split + Auto
--disorder 1 --split 1 --auto=torst

# Стратегия 2: TLS Record Split
--tlsrec 1+s --split 1

# Стратегия 3: Fake packets
--fake 1 --ttl 10

# Стратегия 4: Комбинированная
--disorder 1 --split 1 --tlsrec 1+s
```

---

## ⚙️ Сценарий 4: Zapret на системном уровне

**Задача:** Обработать весь исходящий трафик на уровне iptables

```bash
./level-3-advanced.sh \
  --method zapret \
  --non-ru-ip 95.217.123.45

# Скрипт автоматически:
# ✅ Клонирует Zapret из GitHub
# ✅ Запустит ./install_easy.sh
# ✅ Создаст iptables правила для Non-RU IP
# ✅ Настроит nfqws с параметрами --dpi-desync=split2
```

**Архитектура:**
```
Client → RU VPS (3x-ui) → [iptables NFQUEUE] → nfqws → Non-RU VPS
                                ↑
                          Перехват на
                          системном уровне
```

**Проверка Zapret:**
```bash
# Статус сервиса
systemctl status zapret

# Проверить iptables правила
iptables -t mangle -L POSTROUTING -n -v

# Логи nfqws
journalctl -u zapret -f
```

**Изменение стратегии Zapret:**
```bash
# Редактировать конфиг
nano /opt/zapret/config

# Изменить строки:
NFQWS_OPT_DESYNC="--dpi-desync=split2 --dpi-desync-split-pos=2"
# или
NFQWS_OPT_DESYNC="--dpi-desync=fake --dpi-desync-repeats=6"

# Перезапустить
systemctl restart zapret
```

---

## 🧪 Сценарий 5: Только тестирование (без изменений)

**Задача:** Проверить доступность Non-RU VPS перед применением

```bash
./level-3-advanced.sh \
  --non-ru-ip 95.217.123.45 \
  --test-only

# Вывод:
# [INFO] Тестирование соединения к 95.217.123.45:443...
# [SUCCESS] Соединение успешно установлено

# ИЛИ

# [ERROR] Не удалось подключиться к 95.217.123.45:443
```

---

## 🔄 Сценарий 6: Миграция между методами

**Задача:** Сначала использовали Xray Fragment, хотим попробовать ByeDPI

```bash
# Шаг 1: Откатиться к оригинальной конфигурации
./rollback.sh /root/3x-ui-backups/config-TIMESTAMP.json

# Шаг 2: Применить новый метод
./level-3-advanced.sh \
  --method byedpi \
  --non-ru-ip 95.217.123.45 \
  --auto-strategy

# Шаг 3: Протестировать
# Клиент должен продолжать работать без перенастройки!
```

---

## 📊 Сценарий 7: Несколько Non-RU серверов (балансировка)

**Задача:** Настроить DPI bypass для 3 Non-RU серверов

```bash
# Сервер 1 (Finland)
./level-3-advanced.sh \
  --method xray-fragment \
  --non-ru-ip 95.217.123.45

# Сервер 2 (Netherlands)
./level-3-advanced.sh \
  --method xray-fragment \
  --non-ru-ip 185.125.190.20

# Сервер 3 (Germany)
./level-3-advanced.sh \
  --method xray-fragment \
  --non-ru-ip 78.47.150.30
```

**Ручная настройка балансировки (в конфиге Xray):**
```json
{
  "routing": {
    "balancers": [
      {
        "tag": "non-ru-cluster",
        "selector": ["finland-server", "netherlands-server", "germany-server"],
        "strategy": {
          "type": "leastPing"
        }
      }
    ],
    "rules": [
      {
        "type": "field",
        "inboundTag": ["client-inbound"],
        "balancerTag": "non-ru-cluster"
      }
    ]
  }
}
```

---

## 🛠️ Сценарий 8: Отладка и troubleshooting

**Задача:** Соединение не работает, нужно найти проблему

```bash
# 1. Проверить, работает ли x-ui
systemctl status x-ui

# 2. Проверить логи x-ui
journalctl -u x-ui -f

# 3. Протестировать соединение вручную
timeout 5 bash -c "echo >/dev/tcp/95.217.123.45/443" && echo "OK" || echo "FAIL"

# 4. Проверить, применился ли fragment
cat /usr/local/x-ui/bin/xray-linux-amd64/config.json | jq '.outbounds[] | select(.tag == "fragment")'

# 5. Если используете ByeDPI - проверить SOCKS5
curl --socks5 127.0.0.1:1080 https://google.com

# 6. Если используете Zapret - проверить iptables
iptables -t mangle -L POSTROUTING -n -v | grep 95.217.123.45

# 7. Запустить Level 3 в verbose режиме
./level-3-advanced.sh \
  --method xray-fragment \
  --non-ru-ip 95.217.123.45 \
  --verbose
```

---

## 🎓 Best Practices

### 1. Начните с Xray Fragment
```bash
# Самый простой и производительный вариант
./level-3-advanced.sh --method xray-fragment --non-ru-ip YOUR_IP
```

### 2. Включите автоподбор стратегий
```bash
# Для максимальной стабильности
./level-3-advanced.sh --method xray-fragment --non-ru-ip YOUR_IP --auto-strategy
```

### 3. Сохраняйте бэкапы
```bash
# Перед экспериментами
cp /usr/local/x-ui/bin/xray-linux-amd64/config.json /root/manual-backup-$(date +%s).json
```

### 4. Мониторьте логи
```bash
# Настройте алерты при падении соединения
echo "*/5 * * * * /opt/health-check.sh || echo 'DPI bypass FAILED' | mail -s 'ALERT' admin@example.com" | crontab -
```

### 5. Документируйте рабочую стратегию
```bash
# Когда нашли рабочую комбинацию - сохраните
echo "Working strategy: xray-fragment, packets=tlshello, length=100-200" > /root/working-dpi-strategy.txt
```

---

## 🆘 FAQ

**Q: Какой метод лучше?**
A: Для большинства случаев - Xray Fragment. Если не помогает - попробуйте ByeDPI.

**Q: Нужно ли что-то менять на клиенте?**
A: НЕТ! Клиент продолжает использовать тот же vless:// ключ.

**Q: Как часто переключаются стратегии при --auto-strategy?**
A: Каждые 5 минут проверка. При падении - сразу переключение.

**Q: Можно ли комбинировать методы?**
A: НЕТ. Используйте один метод за раз. Но можно мигрировать между ними.

**Q: Совместимо ли с Level 1/2?**
A: Да, Level 3 работает на RU VPS (entry), Level 1/2 на Non-RU VPS (exit).

**Q: Как откатиться?**
A: `./rollback.sh /root/3x-ui-backups/config-TIMESTAMP.json`

---

## 📞 Поддержка

**Issues:** https://github.com/alche-my/x-ui-settings-update/issues
**Документация:** `./LEVEL-3-ADVANCED.md`
**Основной README:** `../README.md`

---

**Сделано с ❤️ для стабильного интернета**
