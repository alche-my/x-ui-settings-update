# Настройка балансера через 3x-ui панель (UI)

## ✅ Да, балансер можно настроить через UI панель!

Но есть **два способа**:

### Способ 1: JSON (быстрее) ⚡
- Скопировать готовый конфиг
- Вставить в Xray Config
- Сохранить и перезапустить
- ⏱️ Время: **5 минут**

### Способ 2: Через UI вручную (дольше) 🖱️
- Создать каждый outbound отдельно
- Настроить балансер через интерфейс
- Настроить routing rules
- ⏱️ Время: **15-20 минут**

---

## 📋 Способ 1: Через JSON (Рекомендуется)

### Шаг 1: Сгенерируйте конфиг

```bash
./generate-balancer-config.sh
```

Скрипт создаст `/root/byedpi-config/xray-balancer-config.json`

### Шаг 2: Откройте 3x-ui панель

```
http://ваш-ru-сервер:порт-3x-ui
```

### Шаг 3: Перейдите в Xray Config

В меню:
- **Panel Settings** → **Xray Configs**
- Или просто **Config** (зависит от версии)

### Шаг 4: Найдите секцию "outbounds"

Вы увидите что-то типа:
```json
{
  "log": {...},
  "inbounds": [...],
  "outbounds": [
    {
      "tag": "direct",
      "protocol": "freedom"
    },
    {
      "tag": "blocked",
      "protocol": "blackhole"
    }
  ],
  "routing": {...}
}
```

### Шаг 5: Замените outbounds

Скопируйте секцию `"outbounds": [...]` из вашего файла:

```bash
cat /root/byedpi-config/xray-balancer-config.json
```

И **замените** массив outbounds в UI.

### Шаг 6: Добавьте секцию routing

Скопируйте секцию `"routing": {...}` из файла и замените в UI.

### Шаг 7: Сохраните и перезапустите

- Нажмите **Save Config**
- Нажмите **Restart Xray** (или `sudo systemctl restart x-ui`)

---

## 🖱️ Способ 2: Через UI вручную

Если хочешь настроить все через интерфейс (без JSON):

### Часть 1: Создание Outbounds

#### 1. Создайте ByeDPI SOCKS Outbound

1. В панели 3x-ui → **Outbounds** → **Add Outbound**
2. Заполните:
   ```
   Tag: byedpi-socks
   Protocol: SOCKS
   Address: 127.0.0.1
   Port: 1080
   ```
3. **Save**

#### 2. Создайте Outbound для первого Non-RU сервера

1. **Outbounds** → **Add Outbound**
2. Заполните:
   ```
   Tag: non-ru-1-via-byedpi
   Protocol: VLESS
   Address: 185.1.2.3
   Port: 443
   UUID: ваш-uuid-1
   Encryption: none
   Network: tcp
   Security: tls
   ```
3. В разделе **Advanced Settings** найдите **Proxy Settings**
4. Включите Proxy Settings и укажите:
   ```
   Proxy Tag: byedpi-socks
   ```
5. **Save**

#### 3. Повторите для остальных серверов

Создайте аналогичные outbound-ы:
- `non-ru-2-via-byedpi` (IP: 185.4.5.6, UUID: ваш-uuid-2)
- `non-ru-3-via-byedpi` (IP: 185.7.8.9, UUID: ваш-uuid-3)

Для **каждого** обязательно укажите в Proxy Settings → `byedpi-socks`

### Часть 2: Настройка балансировщика

#### Через JSON в Routing (проще):

1. Перейдите в **Panel Settings** → **Xray Configs**
2. Найдите секцию `"routing"`
3. Добавьте балансировщик:

```json
"routing": {
  "domainStrategy": "AsIs",
  "balancers": [
    {
      "tag": "balancer",
      "selector": [
        "non-ru-1-via-byedpi",
        "non-ru-2-via-byedpi",
        "non-ru-3-via-byedpi"
      ],
      "strategy": {
        "type": "random"
      }
    }
  ],
  "rules": [
    {
      "type": "field",
      "network": "tcp,udp",
      "balancerTag": "balancer"
    }
  ]
}
```

4. **Save** и **Restart**

#### Через UI Routing Rules (если есть такая опция):

**Внимание:** Не все версии 3x-ui поддерживают настройку балансера через UI!

Если в вашей версии есть раздел **Routing → Balancers**:

1. Перейдите в **Routing** → **Balancers**
2. Нажмите **Add Balancer**
3. Заполните:
   ```
   Tag: balancer
   Selector: non-ru-1-via-byedpi, non-ru-2-via-byedpi, non-ru-3-via-byedpi
   Strategy: random
   ```
4. **Save**

5. Создайте Routing Rule:
   - **Routing** → **Rules** → **Add Rule**
   - **Type**: Field
   - **Network**: tcp,udp
   - **Balancer Tag**: balancer
   - **Save**

---

## 🎨 Скриншоты UI (что искать)

### Создание Outbound:

```
┌─────────────────────────────────────────┐
│ Add Outbound                            │
├─────────────────────────────────────────┤
│ Tag: [non-ru-1-via-byedpi        ]      │
│ Protocol: [VLESS ▼]                     │
│ Address: [185.1.2.3              ]      │
│ Port: [443                       ]      │
│ UUID: [ваш-uuid-1                ]      │
│ Encryption: [none ▼]                    │
│ Network: [tcp ▼]                        │
│ Security: [tls ▼]                       │
│                                         │
│ ▼ Advanced Settings                     │
│   ☑ Enable Proxy Settings               │
│   Proxy Tag: [byedpi-socks       ]      │
│                                         │
│ [Save]  [Cancel]                        │
└─────────────────────────────────────────┘
```

### Настройка балансировщика в Routing:

```json
{
  "balancers": [
    {
      "tag": "balancer",
      "selector": ["non-ru-1-via-byedpi", "non-ru-2-via-byedpi", "non-ru-3-via-byedpi"],
      "strategy": {"type": "random"}
    }
  ]
}
```

---

## 🔍 Проверка через UI

После настройки:

### 1. Проверьте Outbounds

**Outbounds** → должны быть видны:
- ✅ `byedpi-socks` (SOCKS, 127.0.0.1:1080)
- ✅ `non-ru-1-via-byedpi` (VLESS, с Proxy Settings)
- ✅ `non-ru-2-via-byedpi` (VLESS, с Proxy Settings)
- ✅ `non-ru-3-via-byedpi` (VLESS, с Proxy Settings)

### 2. Проверьте Routing

**Routing Rules** → должно быть правило:
- ✅ Balancer Tag: `balancer`
- ✅ Network: tcp,udp

### 3. Проверьте логи

**Logs** → должны появиться записи о балансировке

---

## ⚠️ Важные моменты

### 1. Proxy Settings обязательны!

Для каждого Non-RU outbound **обязательно** включите Proxy Settings:
```
Proxy Tag: byedpi-socks
```

Без этого трафик пойдет напрямую (без ByeDPI) и будет заблокирован DPI!

### 2. Selector должен содержать правильные теги

В балансировщике укажите точные теги outbound-ов:
```json
"selector": [
  "non-ru-1-via-byedpi",  // Должен совпадать с Tag outbound
  "non-ru-2-via-byedpi",
  "non-ru-3-via-byedpi"
]
```

### 3. Стратегии балансировки

Доступные стратегии:
- `random` - случайный выбор (рекомендуется)
- `leastPing` - наименьший пинг (требует Xray 1.8.0+)
- `leastLoad` - наименьшая нагрузка

---

## 📊 Сравнение способов

| Критерий | JSON (Способ 1) | UI (Способ 2) |
|----------|----------------|---------------|
| Скорость | ⚡ 5 мин | 🐌 15-20 мин |
| Сложность | 😊 Легко | 😐 Средне |
| Ошибки | Меньше | Больше |
| Гибкость | Полная | Ограничена UI |
| Рекомендуется | ✅ Да | Для обучения |

---

## 💡 Рекомендация

**Для продакшена:** Используйте JSON (Способ 1)
- Быстрее
- Меньше ошибок
- Легко версионировать
- Можно автоматизировать

**Для обучения:** Попробуйте UI (Способ 2)
- Понимание структуры
- Визуальная настройка
- Экспериментирование

---

## 🔧 Если что-то пошло не так

### Балансировка не работает

1. Проверьте конфиг через JSON:
   - **Panel Settings** → **Xray Configs**
   - Убедитесь, что `balancers` и `rules` на месте

2. Проверьте логи:
   ```bash
   sudo journalctl -u x-ui -f | grep -i balanc
   ```

3. Перезапустите Xray:
   ```bash
   sudo systemctl restart x-ui
   ```

### UI не показывает балансер

Некоторые версии 3x-ui не имеют UI для балансера.
**Решение:** Используйте JSON (Способ 1)

### Outbound не сохраняется

- Проверьте, что все обязательные поля заполнены
- Проверьте формат UUID
- Попробуйте через JSON

---

## 📚 Дополнительные ресурсы

- **Xray Documentation:** https://xtls.github.io/config/routing.html#balancerobject
- **3x-ui GitHub:** https://github.com/MHSanaei/3x-ui
- **Наш README:** `README-balancer.md`

---

## ✅ Итого

**Да, можешь настроить балансер через UI панель!**

Но проще через JSON:
1. Запусти `./generate-balancer-config.sh`
2. Скопируй JSON в 3x-ui → Xray Config
3. Restart
4. Готово!

Хочешь - могу показать конкретный пример для твоих 3 серверов? 😊
